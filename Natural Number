#include<stdio.h>
int main ()
{   int a;
    printf("Enter a Number :- ");
    scanf("%D",&a);
    if (a>=1 && a<=100)
    {
        printf("It is a Natural Number.");
    }else
    {
        printf("Not a Natural Number");
    }
    return 0;
}
