//W.A.P to Check the password is erong or right ?
#include<stdio.h>
int main()
{
    int n;
do
{   printf("Enter Password :");
    scanf("%d",&n);

    if (n%7 == 0)
    {
        break;
    }
    printf("Wrong Password : ");
} while (1);
    printf("Granted ");

    return 0;
}
