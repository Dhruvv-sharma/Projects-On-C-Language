//W.A funtion to find squareroot of a number ? 
#include<stdio.h>
int sqr(int n);
int main()
{   int n;
    printf("Enter a Number : ");
    scanf("%d",&n);
    printf("Square of %d is %d.",n,sqr(n));
    return 0;
}
int sqr(int n)
{
    return n * n;
}
